{{ template "base" .}}

{{define "content"}}
<div class="container">
            <div class="row">
                <div class="col">
                    <h1>This is the about page!</h1>
                    <p>This is some even more text about us !</p>
                    <p>This come from the template:</p>
                    <p>{{ index .StringMap "test" }}</p>
                </div>
            </div>
        </div>
{{end}}